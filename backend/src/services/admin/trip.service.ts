import { prisma } from "../../config/prisma";
import { AppError } from "../../utils/AppError";

// List all trips page
export const getAllTrips = async () => {};
export const postTrip = async () => {};
// List all delayed trips page
export const getAllDelayedTrips = async () => {};
